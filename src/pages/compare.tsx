import ComparePage from '~/containers/ComparePage'
import Layout from '~/layout'

export default function Compare() {
	return (
		<Layout title={`Compare Chains - `}>
			<ComparePage />
		</Layout>
	)
}
